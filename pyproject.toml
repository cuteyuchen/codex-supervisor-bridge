[project]
name = "codex-supervisor-bridge"
version = "0.1.0"
description = "Persistent supervision and context bridge for ChatGPT and Codex"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
